# cPanel helpers (FACE L7)

| File | Use |
| --- | --- |
| `public_html.htaccess` | When the whole Laravel tree sits under `public_html/` and you cannot change the domain document root to `public/` |
| `../prepare-cpanel.ps1` | Builds a zip for File Manager upload (Windows) |
| `../prepare-cpanel.sh` | Same for macOS/Linux/SSH |

Full steps: [../../DEPLOY.md](../../DEPLOY.md).
